@*É possivel definir um layou padrão criando uma unica vez ao inves de sertar tela por tela
  usando o recurso ViewStart: criar uma pagina do tipo view page, com o nome padrão _ViewStart*@

@{
    Layout = "~/Views/Shared/_LayoutPadrao.cshtml";
}
